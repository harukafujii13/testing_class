export function addTwoNumbers(a: number, b: number): number {
  if (a < 0 || b < 0) {
    return -1;
  }
  return a + b;
}

export function multiplayTwoNumbers(a: number, b: number): number {
  if (a < 0 || b < 0) {
    return -1;
  }
  return a * b;
}
